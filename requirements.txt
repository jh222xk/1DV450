# Django version
django>=1.7

pillow

# Elasticsearch
pyelasticsearch

# API
djangorestframework==3.0.3
djangorestframework-jwt
django-filter
django-cors-headers

# Testing
factory_boy
coverage
selenium
django-nose
coveralls
fake-factory
django-debug-toolbar

# sqlparse should be installed by django-debug-toolbar but
# in some cases it does not so lets include it here.
sqlparse
